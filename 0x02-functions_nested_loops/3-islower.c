#include <stdio.h>

/**
 * main - Entr
 * Return: Always 0 (Success)
 */

int  _islower(int c)
{



	char c;

	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	{
	return (0);
	}

	return (int);

}
