#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Description: 'the program's description
 * Return: Always 0 (Success)
 */
int main(void)
{
	int k;

	for (k = 0; k < 10; k++)
	{
		putchar(k);
	}
	putchar('\n');
	return (0);
}
