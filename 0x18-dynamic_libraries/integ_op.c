#include <stdio.h>
/**
* add - add two interger
* @a: first integer
* @b: second integ
* Return: sum of a & b
*/
int add(int a, int b)
{
	return (a + b);
}
/**
* sub - sub two interger
* @a: first integer
* @b: second integ
* Return: sub of a & b
*/
int sub(int a, int b)
{
	return (a - b);
}
/**
* mul - multwo interger
* @a: first integer
* @b: second integ
* Return: mul of a & b
*/
int mul(int a, int b)
{
	return (a * b);
}
/**
* div - div two interger
* @a: first integer
* @b: second integ
* Return: div of a & b
*/
int div(int a, int b)
{
	return (a / b);
}
/**
* mod - mod two interger
* @a: first integer
* @b: second integ
* Return: mol of a & b
*/
int mod(int a, int b)
{
	return (a % b);
}
