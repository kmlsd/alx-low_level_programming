#include <stdio.h>
/**
 * main - print  pr name
 * @argv: array
 * @argc:no of atgument
 * Return: 0 success
 */

int main(int argc, __attribute__((unused))char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
	}
	printf("%d\n", i - 1);
	return (0);
}
